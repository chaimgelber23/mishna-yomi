name = "mishna-yomi"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# All env vars are set in the Cloudflare Pages dashboard (not here)
# to avoid binding name conflicts.
